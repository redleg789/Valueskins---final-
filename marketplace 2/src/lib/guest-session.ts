// Guest sessions are handled server-side via the unified auth system.
// Unauthenticated users get a temporary session cookie on first visit.
// See: backend/auth_service/src/session.rs and backend/account_service/
export {};
